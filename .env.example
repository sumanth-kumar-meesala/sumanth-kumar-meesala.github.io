# URL of the deployed Groq proxy (see proxy/ and .github/workflows/deploy-proxy.yml).
# Unset = the agent composes answers locally from retrieved facts.
VITE_ASK_ENDPOINT=https://ask-sumanth.<your-account>.workers.dev
